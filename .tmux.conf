# if run as "tmux attach", create a session if one does not already exist
new-session -n $HOST

# Change the Prefix Key
#set -g prefix C-a
#unbind C-b
#bind C-a send-prefix
set -g prefix C-Space
unbind C-b
bind C-Space send-prefix

# Change the Key Bindings: vi mode
set -g status-keys vi
setw -g mode-keys vi


# split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Enable mouse control (clickable windows, panes, resizable panes)
#set -g mouse-select-window on
#set -g mouse-select-pane on
#set -g mouse-resize-pane on
# For tmux version above 2.1; invalid option in 2.2
#setw -g mode-mouse on
#set-option -g mouse-select-pane on


# Correct paste
# set-option -g default-command "reattach-to-user-namespace -l zsh"
# Vim copy mode
# unbind [
# bind Escape copy-mode
unbind p
bind p paste-buffer
bind -t vi-copy 'v' begin-selection
bind -t vi-copy 'C-v' rectangle-toggle
bind -t vi-copy 'y' copy-selection
bind -t vi-copy 'Escape' cancel
#https://blog.petrzemek.net/2016/02/11/my-tmux-configuration/
# above 2.3
#bind -T copy-mode-vi 'v' begin-selection
#bind -T copy-mode-vi 'C-v' rectangle-toggle
#bind -T copy-mode-vi 'y' copy-selection
#bind -T copy-mode-vi 'Escape' cancel



# Start at 1
set -g base-index 1
setw -g pane-base-index 1

setw -g aggressive-resize on

set -g @shell_mode 'vi'
set -g @yank_selection 'primary'


bind-key r source-file /home/zuxfoucault/.zuxfoucaultDotFile/.tmux.conf
bind-key S command-prompt -p ssh: "new-window -n %1 'ssh %1'"
bind-key | split-window -h
bind-key - split-window -v
#unbind-key j
#bind-key j select-pane -D
#unbind-key k
#bind-key k select-pane -U
#unbind-key h
#bind-key h select-pane -L
#unbind-key l
#bind-key l select-pane -R


# Set colors
#set-option -g default-terminal "screen-256color"
#set-option -g default-terminal "xterm-256color"
set-option -g default-terminal "tmux-256color"

# panes
#set -g pane-border-fg black
#set -g pane-active-border-fg brightred


# Set the title bar
set -g set-titles on
set -g set-titles-string '#(whoami) :: #h :: #(curl ipecho.net/plain;echo)'


#set-option -g status-utf8 on
set -g status-bg black
set -g status-fg green
set -g status-left-length 15
#set -g status-left ' 9#[fg=cyan,bright]#10H#[fg=green]:#[fg=white]#S#[fg=green] | #[default]'
#set -g status-left ' #[fg=cyan,bright]#H#[fg=green]:#[fg=colour253]#S#[fg=green] | #[default]'
set -g status-left '#[fg=colour39]#S#[fg=green]|'
#set -g status-right '|#[fg=yellow]%y-%m-%d %H:%M'
set -g status-right '|#[fg=colour166]%H:%M'
#set -g status-right '|#{?client_prefix,#[bg=colour154]%H:%M,#[fg=yellow]%H:%M}'
#set -g status-right '|#{?client_prefix,#[bg=colour212]PFX.#[bg=colour202]#S,#[fg=white,bg=colour203,bold]#S}'
set -g status-justify centre

# messaging
#set -g message-fg black
#set -g message-bg yellow
#set -g message-command-fg blue
#set -g message-command-bg black

#window mode
#setw -g mode-bg colour6
#setw -g mode-fg colour0

# window status
#setw -g window-status-format " #F#I:#W#F "
#setw -g window-status-current-format " #F#I:#W#F "
#setw -g window-status-format "#[fg=magenta]#[bg=black] #I #[bg=cyan]#[fg=colour8] #W "
#setw -g window-status-current-format "#[bg=brightmagenta]#[fg=colour8] #I #[fg=colour8]#[bg=colour14] #W "
#setw -g window-status-current-bg colour0
#setw -g window-status-current-fg colour11
#setw -g window-status-current-attr dim
#setw -g window-status-bg green
#setw -g window-status-fg black
#setw -g window-status-attr reverse

# loud or quiet?
#set-option -g visual-activity off
#set-option -g visual-bell off
#set-option -g visual-silence off
#set-window-option -g monitor-activity off
#set-option -g bell-action none

#set-option -g visual-activity on
set-option -g visual-bell on
set-option -g visual-silence off
#set-window-option -g monitor-activity on
set-option -g bell-action other

# The modes {
setw -g clock-mode-colour colour135
setw -g mode-attr bold
setw -g mode-fg colour196
setw -g mode-bg colour238

# }
# The panes {

#set -g pane-border-bg colour235
#set -g pane-border-fg colour238
#set -g pane-active-border-bg colour236
#set -g pane-active-border-fg colour51

# }
# The statusbar {

set -g status-position bottom
#set -g status-bg colour234
#set -g status-fg colour137
set -g status-attr dim
#set -g status-left ''
#set -g status-right '#[fg=colour233,bg=colour241,bold] %d/%m #[fg=colour233,bg=colour245,bold] %H:%M:%S '
set -g status-right-length 50
set -g status-left-length 20

#setw -g window-status-current-fg colour81
#setw -g window-status-current-fg colour250
#setw -g window-status-current-bg colour238
setw -g window-status-current-attr bold
#setw -g window-status-current-format '#I#[fg=colour246]:#[fg=colour250]#W#[fg=green]#F'

#setw -g window-status-current-format '#{?client_prefix,#[bg=colour76]#[fg=colour20]#I#[fg=colour20]:#[fg=colour20]#W#[fg=colour23]#F,#[fg=colour15]#I#[fg=colour15]:#[fg=colour15]#W#[fg=colour15]#F}'
setw -g window-status-current-format '#{?client_prefix,#[bg=colour76]#[fg=colour17]#I#[fg=colour17]:#[fg=colour17]#W#[fg=colour23]#F,#[fg=colour38]#I#[fg=colour38]:#[fg=colour38]#W#[fg=colour38]#F}#{?window_zoomed_flag,🔍,}'

setw -g window-status-fg colour29
#47

#setw -g window-status-fg colour138
#setw -g window-status-bg colour235
setw -g window-status-attr none
#setw -g window-status-format '#I#[fg=colour247]:#[fg=colour250]#W#[fg=colour244]#F'

setw -g window-status-bell-attr bold
setw -g window-status-bell-fg colour255
setw -g window-status-bell-bg colour1

# }
# The messages {

set -g message-attr bold
set -g message-fg colour232
set -g message-bg colour166

# }


# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Other examples:
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @yank_selection 'primary'
set -g @shell_mode 'vi'
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '/home/zuxfoucault/.tmux/plugins/tpm/tpm'

# Set scrollback buffer size
set-option -g history-limit 3000
